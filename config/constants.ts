// API endpoint
export const API_ENDPOINT = process.env.NEXT_PUBLIC_API_URL || 'https://localhost:5011/api/';

// City assocation id that will be send in most of api requests
export const CITY_ASSOCIATION_ID = 1;
